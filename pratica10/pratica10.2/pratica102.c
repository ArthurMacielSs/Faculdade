#include <stdio.h>
#include <stdlib.h>

struct address {
    char *line;
    int number;
    int zipcode;
};


struct address *create_address(const char *input){
struct address est;

}


void free_address(struct address *addr){


}